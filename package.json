{
  "name": "normalize-error",
  "version": "0.3.0",
  "description": "normalize an error",
  "main": "normalize_error.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "BRIK AS <post@brik.no>",
  "license": "MIT",
  "dependencies": {
    "destroy-circular": "^1.1.0",
    "format-error": "^1.0.0"
  }
}
